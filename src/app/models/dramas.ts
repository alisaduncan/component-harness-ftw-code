export interface Drama {
    id: number;
    name: string;
    description: string;
    image: string;
    tags: string[],
    cast: string[],
    release: string,
    rating: number,
    comments: string[]
}
